machine:
  node:
    version: 0.12
dependencies:
  pre:
    - npm install -g jsdoc
test:
  #override:
   # - npm run coverage
 post:
 #  - jsdoc -c test/conf.json
general:
  artifacts:
    - "./docs"
    - "./coverage"
