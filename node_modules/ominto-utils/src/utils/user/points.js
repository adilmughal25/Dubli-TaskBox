'use strict';

const MAX_POINTS = 500;

function calculateRateFromPoints (points) {
  return 100 + (points > 0 ? parseFloat(((points > MAX_POINTS ? MAX_POINTS : points) / 5).toFixed(2)) : 0);
}

module.exports = {
  calculateRateFromPoints: calculateRateFromPoints
};
