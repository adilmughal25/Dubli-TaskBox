'use strict';

function setIntoResponseBody (modelType, model, ctx) {
  ctx.body = ctx.body || {};
  ctx.body.meta = ctx.body.meta || {};
  ctx.body[modelType] = model;
}

module.exports = setIntoResponseBody;
