'use strict';

let dataClient;

module.exports = function init (config) {
  dataClient = require('../utils/rest-client')(config.data_api, false);
  return {
    getStrings: getStrings
  };
};

function * getStrings (params, ctx) {
  let result = yield dataClient.get('strings', params, ctx);
  return result.body;
}
