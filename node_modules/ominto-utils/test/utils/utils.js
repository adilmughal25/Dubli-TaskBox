'use strict';

const chai = require('chai');
const expect = chai.expect;

const utils = require('../../src/utils');

describe('utils', function () {
  for (var util of [
    'checkApiResponse',
    'createEnvelope',
    'createEnvelopeFromLogger',
    'cloudsearch',
    'coinbaseClient',
    'date',
    'duck',
    'formatCurrency',
    'generateCode',
    'getDataClient',
    'hash',
    'kinesisClient',
    'lambdaHandler',
    'metrics',
    'payuClient',
    'promisePiggyback',
    'promiseRateLimiter',
    'query',
    'schemas',
    'serializers',
    'setIntoResponseBody',
    'splitObjectBySchemaDef',
    'userClient'
  ]) {
    describe(util, function () {
      it('should be made available as a function', () => {
        expect(utils[util]).to.be.a('function');
      });
    });
  }
});
