'use strict';

describe('utils/hash', () => {
  let hash = require('../../src/utils/hash');

  it('should cosistantly hash equivilant objects', () => {
    expect(hash({a:1, b:2})).to.eql(hash({b:2, a:1}));
  });
});
